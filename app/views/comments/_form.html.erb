<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %>
    <%= f.text_field :commenter %><br />
  </p>
  <p>
    <%= f.label :body %>
    <%= f.text_area :body %><br />
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>